package com.borrowhub.backend.config;

public class AppConstants {

    // Same demo-admin gate as the frontend's config.js — sign up with this
    // email to get the ADMIN role and reach the equipment-review endpoints.
    public static final String ADMIN_EMAIL = "admin@borrowhub.com";

    public static final double PLATFORM_FEE_RATE = 0.10; // 10%

    private AppConstants() {}
}