spring.application.name=backend

server.port=8080

# --- MySQL ---
# Create the database first: CREATE DATABASE borrowhub;
spring.datasource.url=jdbc:mysql://localhost:3306/borrowhub?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Kiru@2003
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- JWT ---
app.jwt.secret=change-this-to-a-long-random-secret-before-you-deploy-anywhere-real
app.jwt.expiration-ms=86400000

# --- PayHere (Sandbox) ---
# Get these from your PayHere Sandbox account: https://sandbox.payhere.lk
# Business Settings -> Domains & Credentials
payhere.merchant-id=1237889
payhere.merchant-secret=MTAyMzQ0OTY0MTEwMTUzMjE1MjcyNDc5OTE4NDU1MTkxOTUxMTI3MA==
payhere.checkout-url=https://sandbox.payhere.lk/pay/checkout

# --- App URLs ---
# backend-url MUST be a public URL for PayHere's notify callback to reach it.
# localhost will NOT work ? use a tunnel (e.g. ngrok http 8080) during development
# and put the ngrok https URL here.
app.frontend-url=http://localhost:5173
app.backend-url=http://localhost:8080