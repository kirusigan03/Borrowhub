// Admin status now comes from the backend's user.role (see AuthContext) —
// no more client-side email gate.

export const PLATFORM_FEE_RATE = 0.1 // 10% platform fee on rental subtotal — mirrors the backend's AppConstants