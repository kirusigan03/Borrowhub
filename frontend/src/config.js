// Simple demo-only "admin" gate — sign up/log in with this email to reach
// the /admin equipment-review dashboard. Swap for a real role check once
// there's a backend with proper user roles.
export const ADMIN_EMAIL = "admin@borrowhub.com"

export const PLATFORM_FEE_RATE = 0.1 // 10% platform fee on rental subtotal
